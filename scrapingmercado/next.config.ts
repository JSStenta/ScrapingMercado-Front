import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'static.cotodigital3.com.ar',
      'carrefourar.vtexassets.com',
      'elnenearg.vtexassets.com'
    ],
  },
};

export default nextConfig;
